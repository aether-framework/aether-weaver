<?xml version="1.0" encoding="UTF-8"?>
<!--
  Build-time weaving driver: runs the weaving engine over the compiled classes of a module,
  producing woven class files on disk. This is the recommended way to use Aether Weaver — it
  needs no JVM flag, has no startup cost, and the woven classes are inspectable artifacts.

  The goals are stubs for now; the module exists so the reactor structure is complete and the
  plugin coordinates are reserved.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>de.splatgames.aether.weaver</groupId>
        <artifactId>aether-weaver-parent</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>aether-weaver-maven-plugin</artifactId>
    <packaging>maven-plugin</packaging>

    <name>aether-weaver-maven-plugin</name>
    <description>Maven plugin that weaves compiled classes at build time.</description>

    <prerequisites>
        <maven>3.9</maven>
    </prerequisites>

    <properties>
        <automatic.module.name>de.splatgames.aether.weaver.maven</automatic.module.name>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.jetbrains</groupId>
            <artifactId>annotations</artifactId>
        </dependency>
        <dependency>
            <groupId>de.splatgames.aether.weaver</groupId>
            <artifactId>aether-weaver-engine</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.maven</groupId>
            <artifactId>maven-plugin-api</artifactId>
            <version>${maven.api.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.maven.plugin-tools</groupId>
            <artifactId>maven-plugin-annotations</artifactId>
            <version>${maven-plugin-tools.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.maven</groupId>
            <artifactId>maven-core</artifactId>
            <version>${maven.api.version}</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-plugin-plugin</artifactId>
                <version>${maven-plugin-tools.version}</version>
            </plugin>
        </plugins>
    </build>
</project>
